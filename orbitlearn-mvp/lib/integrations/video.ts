// Placeholder for Mux / Cloudflare Stream helpers.
export async function getSignedPlaybackUrl(publicId: string): Promise<string> {
  // Replace with real provider call. For now, return the publicId as-is.
  return publicId
}
